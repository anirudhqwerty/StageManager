<Window x:Class="StageManager.TodoListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="To-Do List" Height="520" Width="340"
        AllowsTransparency="True" WindowStyle="None" ShowInTaskbar="False"
        Background="Transparent" Topmost="True" ShowActivated="False"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <!-- Modern CheckBox with vector checkmark -->
        <Style x:Key="TodoCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI, Segoe UI Variable"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Box" Width="22" Height="22" CornerRadius="6"
                                    BorderBrush="#50FFFFFF" BorderThickness="1.5"
                                    Background="Transparent" VerticalAlignment="Center"
                                    HorizontalAlignment="Center">
                                <Path x:Name="Tick"
                                      Data="M4,11 L8,15 L17,5"
                                      Stroke="White" StrokeThickness="2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                      Stretch="Uniform" Width="12" Height="10"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Visibility="Collapsed"/>
                            </Border>
                            <TextBlock Grid.Column="1" x:Name="Label"
                                       Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="{TemplateBinding Foreground}"
                                       VerticalAlignment="Center" TextWrapping="Wrap"
                                       LineHeight="20"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="#4CC2FF"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="#4CC2FF"/>
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Label" Property="Foreground" Value="#60FFFFFF"/>
                                <Setter TargetName="Label" Property="TextDecorations" Value="Strikethrough"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="#4CC2FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem Container -->
        <Style x:Key="TodoListItem" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Card" Background="#10FFFFFF"
                                CornerRadius="10" Padding="14,13">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#10FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input TextBox -->
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#12FFFFFF"/>
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="CaretBrush" Value="#EAEAEA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="14,10"
                                BorderBrush="#18FFFFFF" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4CC2FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Button -->
        <Style x:Key="AddBtn" TargetType="Button">
            <Setter Property="Background" Value="#4CC2FF"/>
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                CornerRadius="10" Width="44" Height="40">
                            <TextBlock Text="+" Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Margin="0,-2,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#70D4FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#3AAFEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8,8,0,8" CornerRadius="12,0,0,12"
            Background="#F2202020" BorderBrush="#22FFFFFF" BorderThickness="1,1,0,1">
        <Grid Margin="22,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Margin="0,2,0,0">
                <TextBlock Text="To-Do" Foreground="White" FontSize="21" FontWeight="SemiBold"
                           FontFamily="Segoe UI"/>
                <Border Height="2.5" Background="#4CC2FF" HorizontalAlignment="Left"
                        Width="30" Margin="0,8,0,0" CornerRadius="2"/>
            </StackPanel>

            <!-- Separator -->
            <Border Grid.Row="1" Height="1" Background="#12FFFFFF" Margin="0,16,0,14"/>

            <!-- List -->
            <ListBox Grid.Row="2" x:Name="TodoListBox" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ItemContainerStyle="{StaticResource TodoListItem}"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Task}" IsChecked="{Binding IsCompleted}"
                                  Style="{StaticResource TodoCheckBox}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Input Row -->
            <Grid Grid.Row="3" Margin="0,14,0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewTaskTextBox" Grid.Column="0"
                         KeyDown="NewTaskTextBox_KeyDown"
                         Style="{StaticResource InputBox}"/>
                <Button Grid.Column="1" Click="AddButton_Click"
                        Style="{StaticResource AddBtn}" Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
